{% extends 'base.html.twig' %}

{% block title %}Session : {{ session.name }}{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h2>Session : {{ session.name }}</h2>
    <ul>
        <li> <a href="{{ path('nuggets', {id: session.id, orderInNuggets: questionNumber}) }}">{{ nuggets.name }}</a></li>
        <li> <a href="{{ path('sorp', {id: session.id}) }}">{{ salt.name }}</a></li>
        <li> <a href="{{ path('menus', {id: session.id}) }}">{{ menus.name }}</a></li>
        <li> <a href="{{ path('sum', {id: session.id}) }}">{{ sum.name }}</a></li>
        <li> <a href="{{ path('death_morbol', {id: session.id}) }}">{{ deathMorbol.name }}</a></li>
    </ul>

</div>
{% endblock %}
