{% extends 'base.html.twig' %}

{% block title %} {{ category.nameDisplay }} : {{ session.name }}{% endblock %}

{% block body %}
<div class="container-fluid">

    <div class="text-center">
        <h1 class="text-danger">{{ category.nameDisplay }} : {{ session.name }}</h1>
    </div>
    
    <div class="row">
        <div class="col-2">
            <a href="{{ path('session_show', {id: session.id}) }}">Retour au départ</a>
        </div>
        <div class="col text-center">
        </div>
        <div class="col-2">
        </div>
    </div>

    <div class="col-12 text-center">
        <img src="{{ asset('images/mini-morbol.png') }}" alt="">
    </div>

</div>
{% endblock %}
