{% extends 'base_game.html.twig' %}

{% block title %}{{ category.name }} : {{ session.name }}{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <h2>{{ category.name }} : {{ session.name }}</h2>

    <ul>
        <li> <a href="{{ path('menu_show', {id: session.id, orderInMenu: menuNumber}) }}">{{ menuOne.name }}</a></li>
        <li> <a href="{{ path('menu_show', {id: session.id, orderInMenu: menuNumber}) }}">{{ menuTwo.name }}</a></li>
        <li> <a href="{{ path('menu_show', {id: session.id, orderInMenu: menuNumber}) }}">{{ menuThree.name }}</a></li>
    </ul>

    <a href="{{ path('session_show', {id: session.id}) }}">Retour à l'entrée</a>
    <a href="{{ path('sum', {id: session.id}) }}">Passer à l'addition</a>

</div>
{% endblock %}
