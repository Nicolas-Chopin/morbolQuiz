{% extends 'base.html.twig' %}

{% block title %}Sel ou poivre : {{ session.name }}{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <h2>Sel ou poivre : {{ session.name }}</h2>

    <h3>{{ session.sorpName }}</h3>

    <a href="{{ path('session_show', {id: session.id}) }}">Retour à l'entrée</a>
    
    <a href="{{ path('menus', {id: session.id}) }}">Les Menus</a>
    

</div>
{% endblock %}
